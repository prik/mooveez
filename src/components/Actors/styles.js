import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  image: {
    maxWidth: '90%',
    borderRadius: 10,
    objectFit: 'cover',
    boxShadow: '0 0 1em rgb(90 90 90)',
  },
}));
